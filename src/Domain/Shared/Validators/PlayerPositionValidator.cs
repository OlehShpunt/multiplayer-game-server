using System.Numerics;

namespace Domain;

public static class PlayerPositionValidator
{
    public static bool IsValid(Vector2 moveFromPosition, Vector2 moveToPosition, Scene scene) // NOTE: scene parameter needed for boundary checks
    {
        // TODO: Implement actual validation logic based on scene boundaries and player speed.

        return true;
    }
}
