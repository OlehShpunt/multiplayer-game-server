using System.Numerics;

namespace Domain;

public static class TeleportPositionValidator
{
    public static bool IsValid(Vector2 teleportFromPosition, Scene teleportFromScene)
    {
        // TODO: Validate if teleportation is allowed at teleportFromPosition, i.e., whether the given position is at the teleport's position.

        return true;
    }
}
