using System.Numerics;

namespace Domain;

public static class TeleportPositionResolver
{
    public static Vector2 Resolve(Scene fromScene, Scene toScene)
    {
        // TODO: Implement resolution logic once the client side teleport positioning is finalized.

        Vector2 resolvedPosition = new Vector2(0, 0);

        return resolvedPosition;
    }
}
